import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navigation from "@/components/HEADERS/Navigation";
import { ILink } from "@/components/HEADERS/Navigation";
import {
  HomeIcon,
  MailIcon,
  MenuIcon,
  ScrollText,
  SettingsIcon,
  UserSquare,
} from "lucide-react";
const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
const Links: ILink[] = [
  {
    href: "#",
    label: "home",
    icon: <HomeIcon />,
  },
  {
    href: "#",
    label: "about",
    icon: <UserSquare />,
  },
  {
    href: "#",
    label: "docs",
    icon: <ScrollText />,
  },
  {
    href: "#",
    label: "contact",
    icon: <MailIcon />,
  },
  {
    href: "#",
    label: "settings",
    icon: <SettingsIcon />,
  },
];

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div>
          <Navigation menuIcon={<MenuIcon />} LinkSet={Links} />
        </div>
        <article> {children}</article>
      </body>
    </html>
  );
}
